# Design review — site build 2.5

Full-page screenshots of every page, light and dark, at 1280px wide. `before/`
is the #137 scaffold at commit `bb5a130`; `after/` is this branch.

They exist so the design pass can be reviewed in the pull request without a
preview deploy. Once the operator has approved the look on the deploy itself,
this directory can be deleted — it is a review artefact, not part of the site,
and nothing in the build references it.

Regenerate with the shoot script recorded in the pull request description.
